---
import TrainingCard from "./TrainingCard.astro";
import trainings_api, {type Training} from "../services/trainings_api";

const trainings: Training[] = await trainings_api.list();
---
<section class="relative py-20 bg-[#004171]/5" id="capacitaciones">
  <div class="absolute -right-16 top-16 h-48 w-48 rounded-full bg-[#96AD2F]/15 blur-3xl"></div>
  <div class="container mx-auto px-6">
    <div class="mx-auto flex max-w-3xl flex-col text-center">
      <span class="text-sm font-semibold uppercase tracking-[0.3em] text-[#004171]/70">Capacitaciones</span>
      <h1 class="mt-3 font-display text-3xl font-semibold text-[#0B1B2B] sm:text-4xl">
        Programas que impulsan talento y actualización profesional
      </h1>
      <p class="mt-4 text-base leading-relaxed text-slate-700">
        En nuestro compromiso con la educación, ofrecemos capacitaciones que
        nutren mentes, inspiran descubrimientos y empoderan a cada individuo para
        alcanzar su máximo potencial, construyendo así un futuro brillante y lleno
        de oportunidades.
      </p>
    </div>
    <div class="mt-12 flex flex-wrap -mx-4 -my-8">
      {
        trainings.map((training: Training) => (
            <TrainingCard data={training} />
        ))
      }
    </div>
  </div>
</section>
